# ──────── Core ingestion ────────
pypdf
requests
beautifulsoup4
python-docx

# ──────── Text processing ────────
regex

# ──────── Embeddings (Pi-optimized) ────────
# NOTE: On Pi4, install torch separately first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers

# ──────── Vector database ────────
chromadb

# ──────── LLM client ────────
requests

# ──────── Voice Assistant ────────
SpeechRecognition
# NOTE: PyAudio is installed via apt on Pi:
#   sudo apt install python3-pyaudio
pyttsx3

# ──────── Utilities ────────
tqdm
